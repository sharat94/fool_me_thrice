<%= form_with(model: verb, local: true) do |form| %>
  <% if verb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(verb.errors.count, "error") %> prohibited this verb from being saved:</h2>

      <ul>
      <% verb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :topic_id %>
    <%= form.text_field :topic_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
